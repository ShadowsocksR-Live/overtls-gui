[package]
name = "overtls-gui"
version = "0.1.0"
edition = "2024"

[dependencies]
bitflags = "2.9.4"
dirs = "6.0"
env_logger = "0.11.8"
image = { version = "0.25", default-features = false, features = ["png"] }
log = "0.4.28"
overtls = { version = "0.3.6" }
qrcode = "0.14.1"
rqrr = "0.10.0"
run-as = { version = "1.2.5", default-features = false }
screenshot = { version = "0.0.7", git = "https://github.com/ssrlive/screenshot-rs.git", rev = "36f877f" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
strum = { version = "0.27", features = ["derive"] }
wxdragon = { path = "../../wxdragon/rust/wxdragon" }

[build-dependencies]
embed-manifest = "1.4"
embed-resource = "3.0.6"

[package.metadata.bundle]
# Install cargo-bundle: cargo install cargo-bundle
# macOS Usage: cargo bundle --package overtls-gui --format osx --target x86_64-apple-darwin
name = "overtls-gui"
identifier = "com.github.ssrlive.overtls-gui"
icon = ["assets/main.png"]
category = "Utility"
short_description = "A overtls-gui application for wxDragon"
long_description = """
A overtls-gui application for wxDragon that showcases the capabilities of the wxDragon framework.
It allows users to view images in various formats, demonstrating the integration of the wxDragon framework with
the Rust programming language.
"""
